import React from "react";
import { Header } from "../../styles/header.style";

const { Wrapper, Title } = Header;

const HeaderComponent = () => {
  return (
    <Wrapper>
      <Title>Search Feature</Title>
    </Wrapper>
  );
};

export default HeaderComponent;
